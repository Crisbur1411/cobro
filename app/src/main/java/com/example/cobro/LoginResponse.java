package com.example.cobro;

public class LoginResponse {
    private String token; // Asegúrate que el nombre coincida con el JSON que devuelve la API

    public String getToken() {
        return token;
    }
}